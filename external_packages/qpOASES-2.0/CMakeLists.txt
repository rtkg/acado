PROJECT(qpOASES CXX)

INCLUDE_DIRECTORIES(INCLUDE/)
INCLUDE_DIRECTORIES(INCLUDE/EXTRAS)
FILE(GLOB SOURCES SRC/*.cpp)
ADD_LIBRARY(qpoases ${SOURCES})

FILE(GLOB SOURCES SRC/EXTRAS/*.cpp)
ADD_LIBRARY(qpoases-extras ${SOURCES})


